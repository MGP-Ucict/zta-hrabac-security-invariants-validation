\* Declares the root behavioral specification
SPECIFICATION Spec

\* Declares the strict safety property (invariant) that TLC must prove
INVARIANT PrivilegeIntegrityInvariant

\* Bounds the infinite types to finite sets to enable explicit model checking
CONSTANTS
    Subjects = {sub1, sub2}
    Permissions = {read_data, approve_tx}
